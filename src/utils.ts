export const stringDown = (str: string) => { return str.toLocaleLowerCase().split(" ").join("-") }
export const stringUp = (str: string) => { return str.split("-").map((word) => word.slice(0, 1).toUpperCase() + word.slice(1, word.length)).join(" ") }